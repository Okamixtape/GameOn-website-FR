---
interface Props {
  /**
   * Nom de l'entreprise affiché dans le copyright
   * @default "PIXEL CLASH"
   */
  companyName?: string;

  /**
   * Année du copyright
   * @default new Date().getFullYear()
   */
  year?: number;

  /**
   * Liens réseaux sociaux (optionnel)
   */
  socialLinks?: Array<{
    /** Plateforme (ex: "twitter", "linkedin") */
    platform: string;
    /** URL complète */
    url: string;
    /** Label ARIA descriptif */
    ariaLabel: string;
  }>;
}

const { 
  companyName = "PIXEL CLASH", 
  year = new Date().getFullYear(),
  socialLinks 
} = Astro.props;
---

<footer class="bg-gradient-to-r from-retro-purple via-purple-900 to-blue-900 text-zinc-300 py-6 mt-auto border-t border-retro-blue/30 shadow-lg shadow-retro-blue/10">
  <div class="container mx-auto px-4 sm:px-6">
    <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-4">
      
      <!-- Section copyright -->
      <div class="text-center sm:text-left">
        <p class="text-sm">
          © {year} {companyName}. Tous droits réservés.
        </p>
      </div>

      <!-- Section liens légaux -->
      <nav aria-label="Liens légaux" class="flex flex-wrap justify-center sm:justify-end gap-4 text-sm">
        <a 
          href="/mentions-legales" 
          class="hover:text-retro-blue transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-retro-blue focus:ring-offset-2 focus:ring-offset-zinc-900 rounded px-1"
        >
          Mentions légales
        </a>
        <a 
          href="/politique-confidentialite" 
          class="hover:text-retro-blue transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-retro-blue focus:ring-offset-2 focus:ring-offset-zinc-900 rounded px-1"
        >
          Politique de confidentialité
        </a>
        <a 
          href="/cgu" 
          class="hover:text-retro-blue transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-retro-blue focus:ring-offset-2 focus:ring-offset-zinc-900 rounded px-1"
        >
          CGU
        </a>
      </nav>

      <!-- Section réseaux sociaux (optionnel) -->
      {socialLinks && socialLinks.length > 0 && (
        <div class="flex justify-center sm:justify-end gap-4">
          {socialLinks.map(({ platform, url, ariaLabel }) => (
            <a
              href={url}
              aria-label={ariaLabel}
              target="_blank"
              rel="noopener noreferrer"
              class="text-zinc-400 hover:text-white transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 focus:ring-offset-zinc-900 rounded"
            >
              <!-- Icône placeholder - à remplacer par SVG réel -->
              <span class="sr-only">{ariaLabel}</span>
              <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                <circle cx="12" cy="12" r="10" />
              </svg>
            </a>
          ))}
        </div>
      )}
    </div>

    <!-- Bannière Portfolio -->
    <div class="mt-6 pt-4 border-t border-retro-blue/20">
      <p class="text-center text-xs text-zinc-400">
        💼 <strong class="text-retro-blue">Projet Portfolio</strong> • Design & développement par <a href="https://github.com/Okamixtape" target="_blank" rel="noopener noreferrer" class="text-retro-blue hover:text-retro-purple transition-colors underline">Loup Aubour</a>
        <br class="sm:hidden" />
        <span class="hidden sm:inline">•</span>
        <span class="block sm:inline mt-1 sm:mt-0">L'événement PIXEL CLASH est fictif et créé à des fins de démonstration.</span>
      </p>
    </div>
  </div>
</footer>
